export interface ImageItem {
    image: string;
    description: string;
}

export interface PostInfo {
    image: string;
    title: string;
    category: string;
    date: string;
    content: string;
    idx:number;
}